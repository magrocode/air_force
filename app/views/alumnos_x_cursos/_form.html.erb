<%= form_for(@alumnos_x_curso) do |f| %>
  <% if @alumnos_x_curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alumnos_x_curso.errors.count, "error") %> prohibited this alumnos_x_curso from being saved:</h2>

      <ul>
      <% @alumnos_x_curso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :curso_id %><br />
    <%= f.collection_select :curso_id, Curso.all, :id, :codigo, :prompt => 'Selecciona un Curso' %>
  </div>
  <div class="field">
    <%= f.label :alumno_id %><br />
    <%= f.collection_select :alumno_id, Alumno.all, :id, :rut, :prompt => 'Selecciona un alumno' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
